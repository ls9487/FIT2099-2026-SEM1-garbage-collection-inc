package game.actors;

/**
 * This enum is used to indicate which abilities/capabilities an actor has.
 * Includes abilities like DIRECT_CONSUMER (can eat stuff off the ground).
 *
 * @author echu0057
 */
public enum ActorAbilities {
    WORKER_HOSTILE,
    DIRECT_CONSUMER,
    VENT_ACTIVATOR,
    SLIME_EFFECT_SUSCEPTIBLE,
    PARASITE_EFFECT_SUSCEPTIBLE
}
